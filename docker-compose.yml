version: "3"

services:
  app:
    build: .
    restart: always


  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/
      - ./nginx/configs/:/etc/nginx/sites-enabled/:ro

