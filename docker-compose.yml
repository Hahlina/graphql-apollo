version: "3.8"

services:
  api:
    build: ./server
    container_name: api
    ports:
      - 3000:3000

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - 80:80
    #  - 443:443

